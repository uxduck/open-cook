# Generated by `wrangler types`. Not used for typing (see src/types/cloudflare.d.ts).
worker-configuration.d.ts
.wrangler/
.vercel/

# src/routeTree.gen.ts is generated by the TanStack Router plugin but committed so
# `tsc --build` typechecks without a prior dev/build run.
